// print name using string datatypes.
let userName:string = "rimanshu";
console.log(userName);

// subtract two numbers using decleared variable.
const a:number = 5;
const b:number = 2;
console.log(a-b);

// program to add two numbers.
function add(a:number, b:number):number{
    return a+b;
}
console.log(add(3, 2));

// program to multiply two numbers.
function multiply(a:number, b:number):number{
    return a*b;
}
console.log(multiply(2, 2));