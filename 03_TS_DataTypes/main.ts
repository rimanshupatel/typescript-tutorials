// string
const userName: string = "rimanshu";

// number
const digits: number = 2;

// boolean
let check: boolean;

check = true;

// user information using ts datatypes declearation;

let user: string;
let surName: string | number;
let age: number;
let isLoading: boolean;

user = "john";
surName = "patel"; // either it string or number like :- john1, john2
age = 19;
isLoading = true;

// console user Data with Message;
console.log(`hii, my name is ${user} ${surName} and my age is ${age}`);
